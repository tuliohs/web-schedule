para inserir uma pagina:
    criar a pagina em views
    adiciona-la no layout
        adicionar o layout no views/index caso ele não exista
    adiciona no SlideBar

para adicionar o diplay message
    import DefaultContext from 'constants/data/DefaultContext'
    const { setMessage, } = useContext(DefaultContext);
    .then(c => setMessage({ type: 'sucess', text: c?.data?.message }))
    .catch(e => setMessage({ type: 'danger', text: e?.toString() }))

DEPLOY
remove build folder > rm -r namediretory

Star work in ubuntu
"start": "PORT=6000 react-scripts start",


usar Create ref ao inves de UseRef


1º Estudo	03:00	2º Estudo	24:00:00	3º Estudo	192:00:00	4º Estudo	30